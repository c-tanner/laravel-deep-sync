preset: package

namespace: CTanner\LaravelDeepSync

paths:
  src: src
  resource: resources

factory:
  path: tests/database/Factories

migration:
  path: tests/database/migrations
  prefix: ''

console:
  namespace: CTanner\LaravelDeepSync\Console

model:
  namespace: CTanner\LaravelDeepSync\Tests\Models

provider:
  namespace: CTanner\LaravelDeepSync\Providers

testing:
  namespace: CTanner\LaravelDeepSync\Tests